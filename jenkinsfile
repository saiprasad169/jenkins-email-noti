node {
     def app
     stage('Deploy') {
         sh test.sh
         }
     stage('email') {
     step([$class: 'Mailer', notifyEveryUnstableBuild: true, recipients: '', sendToIndividuals: false])
 }
